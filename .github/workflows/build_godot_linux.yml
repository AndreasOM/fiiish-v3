name: 🐧 Linux - 🔨 Build
on: 
  workflow_dispatch:
  push:
    tags:
      - "v*"
    # We always build on a tag
    # paths:
    #  - godot/**

jobs:
  build:
    uses: ./.github/workflows/build.yaml
    with:
      project: 'fiiish-v'
      build_type: 'linux'
    secrets: inherit
